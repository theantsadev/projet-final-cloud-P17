import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.antananarivo.signalement',
  appName: 'Signalement Routier',
  webDir: 'dist'
};

export default config;
